{% set name = "nine" %}
{% set version = "1.2.0" %}
{% set sha256 = "352a7dfe11953b7f4d128a847ae01ee74b1d9dfc952f82f3cb1f1dbd41e66ca9" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz"
  sha256: {{ sha256 }}

build:
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps --ignore-installed -vv "
  noarch: python

requirements:
  host:
    - pip
    - python
  run:
    - python

test:
  imports:
    - nine

about:
  home: "https://github.com/nandoflorestan/nine"
  license: "Public domain"
  license_family: "PUBLIC-DOMAIN"
  license_file: "LICENSE.rst"
  summary: "Python 2 / 3 compatibility, like six, but favouring Python 3"
  doc_url: "https://pypi.org/project/nine/"
  dev_url: "https://github.com/nandoflorestan/nine"

extra:
  recipe-maintainers:
    - looooo
